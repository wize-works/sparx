apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: sparx-prod
resources:
  - web.yaml
  - api-rest.yaml
  - api-graphql.yaml
  - dashboard.yaml
# Add per-app manifests here as they ship to Artifact Registry.
# Templates available:
#   - api-mcp     (copy from api-rest.example.yaml)
#   - storefront  (copy from api-rest.example.yaml)
