# CRM Audit — 2026-05-29

**Target:** `https://app.sparx.works` (production)
**Tenant:** `e2e-store` (seed tenant — the only one logged in; treated as the test bed)
**Logged in as:** `e2e-staff@sparx.test` (E2E Staff)
**Driver:** Playwright MCP (Chrome MCP was not available this session)
**Auditor:** Claude Opus 4.7 (1M context)

The audit was authored against the local-dev surface in `docs/crm-audit-prompt.md`; this run pivoted to prod at the user's direction. Mutation flows were aborted mid-walk because the dashboard's module gate is broken in a way that immediately blocks every CRM write server action (see **F-01**). The user's instruction was to file the audit now, treat the module-gate fix as priority #1, and not start patching.

## Summary

- **Routes targeted by prompt:** 34 (28 dashboard browser routes + 6 non-browser surfaces)
- **Routes walked:** 14 dashboard routes (full surface scan), 1 cross-cutting validation submit
- **Routes deferred:** 14 dashboard routes blocked by F-01 (every mutation flow + every `/[id]` detail route — there are no seeded customers, deals, b2b accounts, orders, or quotes I could reach, and write actions are blocked)
- **Non-browser surfaces:** all 6 deferred — REST API requires an issued API key (issuance is itself a mutation flow blocked by F-01)
- **Pass:** 11 routes
- **Fail:** 8 numbered (3 critical, 1 high, 3 medium, 1 low) — see Fix log below for which have shipped patches in this session
- **UX violations:** 5 (no functional break)
- **Known parked:** `api-mcp` service (SSD quota), `mcp.sparx.works` returns 502 — confirmed externally; not a regression

## Fix log (applied in this session, pending re-test)

Worked the fail list in a second pass after the audit handoff. Status as of this commit:

| #    | Status                         | One-line                                                                                                                                                                                                                                                                                                    |
| ---- | ------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F-01 | **structural fix shipped**     | Module gate moved into `crm/layout.tsx` (one place gates every CRM route); duplicate gate checks removed from `/crm/page.tsx` + `/crm/duplicates/page.tsx`; `setModuleEnabledAction` now also calls `revalidatePath('/${slug}', 'layout')` so the gate flip propagates to the toggled module's layout.      |
| F-02 | **retracted (false positive)** | My probe used `getComputedStyle(document.documentElement)` which sits **above** the `<ModuleProvider module="crm">` wrapper. Re-running the probe against an element **inside** the wrapper (any CRM `h1`) returns `#06B6D4`. ModuleProvider correctly sets cyan; the brand color is fine.                  |
| F-03 | **retracted (false positive)** | The form's own CardDescription reads "Only the type is required — the rest is fillable later." The product treats name and email as optional; the audit prompt was the side that drifted (it assumed name + email were required). Not a bug in the form.                                                    |
| F-04 | **fix shipped**                | `loadActiveModules` on the dashboard home now reads each module's `enabled` flag via `isModuleEnabled` against `tenant.settings.modules` instead of hardcoding CMS only. Inactive modules render in the Discover grid; active modules in the Active grid with per-module metric (CMS pages, CRM customers). |
| F-05 | **fix shipped**                | Added `Orders` and `Quotes` to `CrmTabs` (`apps/dashboard/app/(dashboard)/crm/_components/crm-tabs.tsx`); wired the `<CrmTabs current="orders" \| "quotes">` render into `apps/dashboard/app/(dashboard)/crm/orders/page.tsx` and `apps/dashboard/app/(dashboard)/crm/quotes/page.tsx`.                     |
| F-06 | **open (copy decision)**       | "OPEN TASKS" vs "Overdue tasks" — semantic decision (Overdue is more actionable for a dashboard widget but the report query would need to flip). Need product input.                                                                                                                                        |
| F-07 | **open (copy decision)**       | "Built-in" vs "System" badge — pick one wording across the spec and the component.                                                                                                                                                                                                                          |
| F-08 | **architectural note**         | No code change. Documents that the dashboard runs all data fetching through Next.js Server Actions and the public REST API has no CORS for the dashboard origin. External API consumers use API keys; the dashboard never makes browser-side fetches. Should be documented explicitly.                      |

### Files changed in this fix pass

- `apps/dashboard/app/(dashboard)/crm/layout.tsx` — async layout with `isModuleEnabled` gate + ModuleStub fallback
- `apps/dashboard/app/(dashboard)/crm/page.tsx` — removed duplicate gate (now handled by layout)
- `apps/dashboard/app/(dashboard)/crm/duplicates/page.tsx` — removed duplicate gate + `redirect('/crm')` (layout handles it)
- `apps/dashboard/app/(dashboard)/settings/modules/actions.ts` — added `revalidatePath('/${slug}', 'layout')` after toggle
- `apps/dashboard/app/(dashboard)/page.tsx` — replaced hardcoded `loadActiveModules` with one that reads real flags + per-module metric
- `apps/dashboard/app/(dashboard)/crm/_components/crm-tabs.tsx` — added Orders and Quotes tabs
- `apps/dashboard/app/(dashboard)/crm/orders/page.tsx` — render `<CrmTabs current="orders" />` at top of page
- `apps/dashboard/app/(dashboard)/crm/quotes/page.tsx` — render `<CrmTabs current="quotes" />` at top of page

Typecheck passes (`pnpm --filter @sparx/dashboard typecheck`). Mutations against prod still need a re-test pass to verify F-01's structural fix actually unblocks the audit's deferred flows; some of the originally observed F-01 symptoms (e.g. `/settings/modules` showing all 8 modules Inactive after a successful activate) could ALSO be a cross-replica LRU cache lag — the dashboard runs >1 replica in prod and each replica has its own in-process `module-gate.ts` LRU with 60s TTL. If the activation lands on Replica A and the next page request lands on Replica B that cached `enabled=false` pre-activation, B keeps the stale value until its TTL expires. The structural fix above doesn't resolve that race; the proper fix is to publish a `module.activated` Pub/Sub event that every replica subscribes to and uses to clear its own LRU. That's already noted in `packages/auth/src/module-gate.ts` as a Phase 2+ change and was not done in this session.

## Fail list

### F-01 — Module gate is inconsistent across CRM surfaces — **this is the #1 fix**

- **Severity:** critical / **blocker**
- **Route(s) affected:**
  - `/crm` — renders ModuleStub when CRM is Active (should render the customer list)
  - `/crm/duplicates` — server-redirects to `/crm` → falls into the same ModuleStub
  - customer-create Server Action — rejects with `Module "crm" is not active for this tenant` immediately after a successful Activate click on `/settings/modules`
  - `/crm/pipelines`, `/crm/segments`, `/crm/tasks`, `/crm/b2b`, `/crm/reports`, `/crm/orders`, `/crm/quotes` — render full CRM UI **even when `/settings/modules` reports all 8 modules Inactive** (i.e., gate is _not_ enforced on the sub-routes)
- **Repro:**
  1. Sign in to `https://app.sparx.works` as `e2e-staff@sparx.test`.
  2. Visit `/settings/modules`. All 8 modules show **Inactive** status pills, each with an `Activate` button.
  3. Click `Activate` on CRM. Status pill flips to **Active**; button becomes `Deactivate`. Bootstrap fires — `/crm/pipelines` lists the seeded Sales Pipeline with 6 stages, `/crm/segments` lists the 4 built-ins.
  4. Navigate to `/crm`. The page renders the ModuleStub ("CRM is coming online", "Planned" feature cards) — _not_ the customer list. Hard reload + `Cache-Control: no-cache` — same result.
  5. Navigate to `/crm/customers/new`. The form renders. Fill First name + a deliberate bad email. Click `Create customer`. HTML5 client validation fires on the email field; the form still POSTs to the server action; the server returns role=alert: `Module "crm" is not active for this tenant`.
  6. Navigate back to `/settings/modules`. CRM has reverted to **Inactive** (all 8 modules now show Inactive again — even though I never deactivated). Hard reload confirms.
  7. Navigate to `/crm/pipelines` — still renders the Sales Pipeline card with full CRM UI despite the gate now reporting Inactive.
- **Screenshot:** [docs/crm-audit/F-02-crm-root-modulestub-when-active.png](crm-audit/F-02-crm-root-modulestub-when-active.png) (captured under the old F-02 ID before re-numbering; same evidence — `/crm` showing ModuleStub while CRM was Active in `/settings/modules`).
- **Suspected cause:** at least two independent module-state sources in the dashboard, and the write-side Server Action source disagrees with the read-side Server Component source:
  - The Server Action at `customers/new`'s submit handler reads the gate and sees Inactive.
  - The Server Component at `/crm/page.tsx` also reads from a source that doesn't see CRM as Active immediately after activation.
  - The Server Components at `/crm/pipelines/page.tsx`, `/crm/segments/page.tsx`, etc. don't consult the module gate at all (or consult one that always says Active for tenants that have ever had the seeds).
  - The `/settings/modules` page's status pill reads yet another source that drops back to Inactive on the next page load.
    Likely candidates: (a) the dashboard pod's in-process LRU module-gate cache (CLAUDE.md mentions 60s TTL; that didn't help — observed 2+ min after activation), (b) Next.js Route Cache / Data Cache not invalidated when `module.activated` fires, (c) the activation Server Action returning success without actually writing to `tenant_modules` (or writing with a TTL).
- **Suggested fix:**
  1. **Audit what each of these surfaces calls to determine "is module X active for tenant T":** `/crm/page.tsx`, `/crm/pipelines/page.tsx`, `/crm/customers/new`'s create Server Action, `/settings/modules/page.tsx`, and the Activate/Deactivate Server Action. There should be exactly one function. If there are multiple, consolidate. If there is one, audit whether each call site bypasses or short-circuits it.
  2. **Confirm `tenant_modules` is actually written.** Watch the row before/after an Activate click. If the row updates and reverts later, find the process reverting it. If the row never updates, the Server Action is lying about success.
  3. **Invalidate Next.js caches on activation.** The `module.activated` consumer should call `revalidatePath('/crm', 'layout')` (and equivalent for every gated module) so server-rendered route segments pick up the new state on the next request.
  4. **Decide gate enforcement on `/crm/(sub-routes)`.** Right now most sub-routes happily render even when the gate reports Inactive. Per CLAUDE.md the contract is "Disabled modules return 404 from their API surface, run no consumers, and store no rows" — the dashboard surface should match: an inactive module's sub-route must hit the ModuleStub (or 404), not query and render `pipelines` / `segments`.
- **Audit impact:** this blocks every mutation flow in the prompt — create customer, edit note (append-only), drag deal, Credit Hold toggle, segment save+materialize, API-key issue/revoke, module deactivate/reactivate, /welcome onboarding follow-through. It also blocks every non-browser surface (REST `/v1/crm/*`, GraphQL, MCP, webhooks, OpenAPI) because they all require an API key issued via `/settings/ai-integrations`, and key issuance is a write Server Action subject to the same gate.

### F-02 — `--module-active` resolves to Sparx Indigo `#6366f1` on every CRM page, not Cyan `#06B6D4`

- **Severity:** high (brand regression visible to every merchant who turns on CRM)
- **Route(s):** every `/crm/*` page tested — `/crm`, `/crm/pipelines`, `/crm/pipelines/[id]`, `/crm/pipelines/[id]/edit`, `/crm/segments`, `/crm/segments/[id]`, `/crm/tasks`, `/crm/b2b`, `/crm/reports`, `/crm/orders`, `/crm/quotes`, `/crm/customers/new`
- **Repro:** on any `/crm/*` page run `getComputedStyle(document.documentElement).getPropertyValue('--module-active').trim()` in the console. Result: `#6366f1`. Per `docs/sparx-brand-guide.md` + `CLAUDE.md`, CRM color is Cyan `#06B6D4`.
- **Suspected cause:** the `<ModuleProvider module="crm">` wrapper that should set `--module-active` per-route is missing from `apps/dashboard/app/(dashboard)/crm/layout.tsx`, OR the module→color token map doesn't include the `crm` entry and falls through to the default Storefront indigo, OR the layout wraps the routes but ModuleProvider's color resolver has a bug.
- **Suggested fix:** check the CRM route group layout includes `<ModuleProvider module="crm">`; verify `packages/ui/src/providers/module-provider.tsx` includes `crm → #06B6D4` in its color map (and that the `tokens.css` `--module-crm` token is defined).

### F-03 — `/crm/customers/new` has zero required-field markers

- **Severity:** medium (accessibility + UX violation per audit standards)
- **Route:** `/crm/customers/new`
- **Repro:** inspect every form input on `/crm/customers/new`. `input#firstName`, `input#lastName`, `input#email`, `input#phone`, `input#company`, `input#jobTitle`, `textarea#tags`, `input#doNotContact`, `select#type` all have `required="false"` and `aria-required=null`. The labels carry no `*` or "Required" text. The audit standard says "Form fields show `*` or 'Required' on required ones; validation errors appear inline next to the offending field, not only at top of form."
- **Suspected cause:** the form component renders all `<Label>` elements without the `required` prop, and the underlying inputs don't propagate Zod schema `required: true` to the DOM.
- **Suggested fix:** drive the Label `required` prop from the Zod schema's `optional()` shape; add `aria-required="true"` on the inputs themselves. Audit standard wants inline (not top-of-form) validation errors too — verify on the next pass once F-01 is unblocked.

### F-04 — Home dashboard "active modules" count disagrees with /settings/modules

- **Severity:** medium
- **Route:** `/` (dashboard home) vs `/settings/modules`
- **Repro:** sign in fresh. `/` renders an "Active modules" panel reading "1 of 8 modules active" and shows a CMS active card with "0 pages". Navigate to `/settings/modules` immediately — every module including CMS shows the Inactive status pill.
- **Suspected cause:** the home dashboard "Active modules" panel reads from a different source than `/settings/modules` — likely a hardcoded preview card or a cache that stale-survives module-state changes.
- **Suggested fix:** drive the home panel from the same data source as `/settings/modules`; if it's a marketing/preview placeholder, label it as such or remove until real state is wired.

### F-05 — Orders and Quotes are not in the CrmTabs sub-nav

- **Severity:** medium (UX / discoverability)
- **Routes:** `/crm/orders`, `/crm/quotes` (and their `/new` + `/[id]` children)
- **Repro:** load any `/crm/*` page that renders the CrmTabs sub-nav. The tabs are: Customers, Pipelines, Segments, Tasks, B2B accounts, Reports, Duplicates. `/crm/orders` and `/crm/quotes` are not in the list. Navigate directly to either URL — both render functional list pages (H1 "Orders" / "Quotes", 0-items empty state with copy), but the CrmTabs sub-nav itself is missing on those pages.
- **Suspected cause:** Orders/Quotes route segments live outside the CrmTabs layout, and the tabs list itself was authored before those routes existed.
- **Suggested fix:** decide whether Orders/Quotes are first-class CRM tabs (add them) or are only meant to be deep-linked from customer/deal detail (then remove the `/crm/orders/...` / `/crm/quotes/...` URLs from public navigation and only expose them via context).

### F-06 — Reports stat #4 is "Open tasks", prompt called for "Overdue tasks"

- **Severity:** low (copy/spec mismatch; could be either side that's wrong)
- **Route:** `/crm/reports`
- **Repro:** the 4-stat row renders CUSTOMERS=0, B2B ACCOUNTS=0, OPEN DEALS=0, OPEN TASKS=0. Audit prompt §25 specified "4-stat row renders (customers, B2B accounts, open deals, overdue tasks)".
- **Suspected cause:** either the implementation deviated from spec or the spec drifted. Overdue is more actionable than open for an at-a-glance metric.
- **Suggested fix:** decide which is right per product intent. If overdue is wanted, change the report query + label. If open is wanted, update `docs/swift-enchanting-puffin.md` (the implementation plan).

### F-07 — Built-in segment badge text is "Built-in", prompt called for "System"

- **Severity:** low (copy/spec mismatch)
- **Route:** `/crm/segments`
- **Repro:** the four built-in segments each carry a `Built-in` badge. Audit prompt §11 specified the `System` badge.
- **Suggested fix:** same as F-06 — pick one wording and align the spec and the component.

### F-08 — api.sparx.works exposes no CORS for app.sparx.works, no `/api/*` proxy on app.sparx.works

- **Severity:** low (architectural — not necessarily a bug)
- **Routes:** all `/v1/crm/*` on `https://api.sparx.works`
- **Repro:** from the dashboard at `https://app.sparx.works`, `fetch('https://api.sparx.works/v1/crm/customers')` fails with `No 'Access-Control-Allow-Origin' header`. `fetch('/api/v1/crm/customers')` on app.sparx.works returns 404 HTML (Next.js not-found).
- **Implication for this audit:** REST API smoke (audit prompt §29) cannot be exercised from a logged-in browser session. It needs `curl -H "Authorization: Bearer sk_live_..."` from a terminal, and the API key issuance flow at `/settings/ai-integrations` is itself a write blocked by F-01. So the REST smoke is fully deferred.
- **Suggested fix:** if the dashboard never needs client-side fetches, document that explicitly (and that external API consumers must use issued API keys). If it eventually will, add CORS for `app.sparx.works → api.sparx.works`.

## Pass list

- **`/settings/modules`** ✓ — all 8 modules listed with descriptions and Activate/Deactivate buttons; the activation _click_ surface works (the persistence/propagation story is F-01); no confirmation on Activate (acceptable — Activate is constructive); **no visible success toast** within 600ms (UX nit per audit standard).
- **`/crm/pipelines`** ✓ (mod-gate caveat per F-01) — 1 pipeline (Sales Pipeline, Default badge, slug `sales`); 6-stage mini-funnel with probabilities; Show archived link + New pipeline + Edit pipeline + List/Forecast/Open Kanban switcher.
- **`/crm/pipelines/[id]` (Kanban)** ✓ (mod-gate caveat per F-01) — H1 "Sales Pipeline"; 6 columns render (Lead, Qualified, Proposal Sent, Negotiation, Closed Won, Closed Lost); view switcher (Kanban / List / Forecast) wired. No deals seeded so drag-drop could not be exercised.
- **`/crm/pipelines/[id]/edit`** ✓ (mod-gate caveat) — H1 "Edit Sales Pipeline"; 24 inputs across 6 stages (name + probability number + stageType select per row); drag/reorder copy present; Archive pipeline button present (no Delete — correct since stages with deals cannot be deleted). UX nit: each stage has its own Save button (per-stage save). Did not click Save (would hit F-01).
- **`/crm/segments`** ✓ — 4 built-in segments (At Risk, B2B Fleet, High Value, New Customers), each tagged `Built-in` (see F-07), slug + description + 0 members + Open link. Page-level Recompute / New segment / Show archived CTAs. (Per-segment Recompute that the audit prompt called for is not present — only top-of-page Recompute.)
- **`/crm/segments/[id]` (At Risk built-in)** ✓ — H1 "At Risk"; Rule + Members 0 sections; Clone + Edit links present. Edit link exists for a built-in (audit prompt says built-ins shouldn't be editable, only cloneable — I did not click through to see whether the form gates writes, since that would hit F-01).
- **`/crm/tasks`** ✓ — H1 "Tasks", `0 open`; scope tabs `My tasks` / `Team tasks`; New task CTA; "No open tasks" empty state with helpful copy. Gap: audit prompt expects Open/Overdue/Completed sections each with counts — only "Open 0" renders on an empty list. Could not verify with content because F-01 blocks task creation.
- **`/crm/b2b`** ✓ — H1 "B2B accounts"; empty state. Gap: audit prompt expects status filter chips (active / credit_hold / suspended / inactive) — not visible on an empty list. Could not verify with content.
- **`/crm/reports`** ✓ — H1 "Reports"; 4-stat row (see F-06 for the OPEN TASKS vs Overdue tasks naming); 3 sections present: "Pipeline funnel — Sales Pipeline", "Win/loss by rep", "Customer acquisition (last 12 months)". All sections render with the empty-tenant zero state.
- **`/crm/orders`** ✓ (mod-gate caveat; sub-nav gap per F-05) — H1 "Orders", `0 orders` empty state. Sales/payment language reads correctly ("Customer orders — placed, paid, fulfilled, delivered, refunded").
- **`/crm/quotes`** ✓ (mod-gate caveat; sub-nav gap per F-05) — H1 "Quotes", `0 quotes` empty state. Lifecycle copy present ("Sales quotes — draft, submitted, accepted, ...").

## UX violations (no functional break, but slop)

- `/settings/modules` — Activate click produces no success toast within 600ms (audit standard wants Saved/Updated indicator on every mutation).
- `/crm/segments` — `Recompute` is only available as a page-level button. Audit prompt §11 called for per-segment Recompute. The current placement also makes it ambiguous which segments are being recomputed.
- `/crm/segments/[id]` — built-in segments expose an Edit link even though the audit prompt §13 says "Cannot edit built-in (`isBuiltIn=true`) — clone first." I did not click through to see whether the form gates writes server-side.
- `/crm/pipelines/[id]/edit` — per-stage Save buttons (6 of them on the default pipeline) instead of a single batch Save. Increases the chance of partial saves and inconsistent stage state.
- `/crm` "Module preview" — the ModuleStub card grid uses an `Module preview` chip and "Planned" labels on every feature card. When this surfaces incorrectly (per F-01) the merchant sees a regression to a coming-soon screen they already left behind; consider letting the stub render a more recoverable affordance ("looks like CRM may have just deactivated — open Settings → Modules"). Marketing for an enabled module shouldn't be on the customer landing.

## API / GraphQL / MCP / Webhook / Cron checks

- **REST `/v1/crm/*`** — **deferred.** Needs an API key from `/settings/ai-integrations` which is itself a write blocked by F-01. CORS gap (F-08) means I cannot smoke from the browser. To exercise: unblock F-01, issue a key, then `curl -H "Authorization: Bearer sk_live_..." https://api.sparx.works/v1/crm/customers`.
- **GraphQL `/v1/graphql`** — **deferred** (same blocker as REST).
- **MCP** — **parked.** `mcp.sparx.works` returns 502 (api-mcp scaled to 0 replicas pending SSD_TOTAL_GB quota bump per CLAUDE.md). Confirmed external behavior; not a regression. Local-dev smoke was not run in this session (audit pivoted to prod).
- **Webhook fan-out** — **deferred.** Requires a CRM write to drive an event; F-01 blocks every write.
- **Cron jobs / partitions** — **deferred.** Read-only `kubectl get cronjobs -n sparx-prod` requires cluster context I did not load this session; partition checks via Cloud SQL would violate the no-touch CORE rule (and the audit prompt explicitly forbids it).
- **OpenAPI `/v1/openapi.json`** — **deferred.** Would have been the first thing I curled if I had an API key. Trivially reachable from any terminal once F-01 is unblocked.
- **Bootstrap event (module.activated → default pipeline + 4 segments)** — **PASS**, observed end-to-end during the Activate click. This is the one server-side fact the audit confirmed positively: the bootstrap consumer ran, the seeds materialized, and they remained queryable even after the module-gate appears to have dropped back to Inactive (more evidence that gate state and DB state are decoupled — see F-01).

## Notes

- This audit was scoped against `https://app.sparx.works` (production) at the user's direction, not against the local dev environment the prompt was originally written for. The seeded e2e-store tenant exists on prod and was the only safe test bed.
- Mutations were authorized in advance for this tenant; F-01 blocked all of them before I could exercise any.
- The Playwright MCP session is using the persistent Chrome profile at `C:\Users\brand\AppData\Local\ms-playwright\mcp-chrome-7520edd`. Seven orphaned Playwright Chrome processes from a previously-aborted workflow setup were killed at the start of this run.
- `apps/dashboard` runs on port 3001 in dev (prompt said 3000) — minor and not relevant to a prod run but worth flagging if the original prompt is rerun locally.
- The favicon is missing — `/favicon.ico` returns 404. Cosmetic; not on the fail list.
- The `mcp.sparx.works` parked state is honored elsewhere in the dashboard — nothing in CRM links to it without explanation. Good.
- Audit was filed without code changes on the first pass. On the second pass (after explicit "let's fix what we found" direction) the patches enumerated in the **Fix log** above were applied. Standing CORE rules respected throughout: no kubectl mutations, no Cloud SQL touches, no stash, no --no-verify, no Co-Authored-By trailers.

## What's needed to fully close this audit

1. **Deploy the F-01 + F-04 + F-05 patches** (see Fix log) and re-test on prod. The structural fixes consolidate the gate but the cross-replica cache race is still open — if the original F-01 symptom reproduces after deploy, the next step is to wire `module.activated` through Pub/Sub so every dashboard replica drops its LRU on activation (already flagged in `packages/auth/src/module-gate.ts` as a Phase 2+ change).
2. Once F-01 verifies, run the deferred mutation phase (create customer → deal → segment → API key issue/revoke → deactivate/reactivate) and the non-browser smoke (REST, GraphQL, webhook fan-out, OpenAPI) — all should unblock in one pass.
3. F-06 + F-07 are copy decisions, not bugs — pick the wording you want and align spec and code.
4. F-08 is an architectural documentation note — decide whether the dashboard ever needs client-side fetches (then add CORS) or never will (then document that public consumers must use API keys).

---

## Round 2 — verification walk (2026-05-30)

Resumed after the Round 1 fixes shipped (commits `72290c7`, `6f64940`, `7188df2`, `3e81ceb`, `be3c333`, `3673c5e` → tags `v0.37.x` and `v0.38.x`). Drove the full deferred mutation phase end-to-end on `https://app.sparx.works` plus a non-browser smoke against REST / GraphQL / MCP.

### Verified fixes (shipped + retested)

| #    | Status        | Evidence                                                                                                                                                                                                                                                                                                                                                                                                              |
| ---- | ------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F-01 | **verified**  | Activated CRM via `/settings/modules`; `/crm`, `/crm/pipelines`, `/crm/segments`, `/crm/b2b` all rendered correctly. Deactivated → `/crm` flipped to ModuleStub (H1 "CRM", 6 feature cards: Customer profiles, Pipeline, Segments, Automation, Activity log, MCP integration). Reactivated → tabs returned (Customers, Segments, Pipelines, Tasks, B2B accounts, Orders, Quotes). Persistence held across navigation. |
| F-04 | **verified**  | Home dashboard `/` now reads `tenant.settings.modules.<slug>.enabled` for all 8 modules via `isModuleEnabled`. Active modules surface with per-module metric (CRM = customer count, CMS = page count) in the Active grid; inactive modules sit in Discover.                                                                                                                                                           |
| F-05 | **verified**  | Orders + Quotes tabs render on every `/crm/*` page; `<CrmTabs current="orders" \| "quotes">` wired in their `page.tsx`.                                                                                                                                                                                                                                                                                               |
| —    | **bonus fix** | `jsonb_set` silent-no-op bug on `setModuleEnabledAction` — Postgres `jsonb_set` cannot create a missing parent, so the first activation on a tenant whose `settings.modules.<slug>` slot didn't exist yet returned UPDATE 1 but persisted nothing. Replaced with a Prisma read-modify-write (commit `7188df2`). User-confirmed end of the silent-failure cycle.                                                       |
| —    | **bonus fix** | Toast feedback on module toggle (`6f64940`) — try/catch around the Server Action call + `toast.success` / `toast.error` on both branches. UX violation #1 in the Round 1 list closed.                                                                                                                                                                                                                                 |

### F-12 — Sparx AlertDialog replaces native `window.confirm` (new — fixed in this round)

- **Severity:** medium (UX consistency + brand)
- **Routes affected (10 call sites):** `/crm/b2b/[id]` (Credit Hold toggle), `/crm/segments` + `/crm/segments/[id]` (Recompute), `/crm/pipelines/[id]/edit` (Archive pipeline), `/settings/ai-integrations` (Revoke key), `/cms/media/[id]` (Delete asset), `/cms/types/[typeKey]/[id]` (Soft-delete entry), `/commerce/categories` (Delete category), `/commerce/products/[id]` (Remove fitment rule, Archive variant)
- **What changed:** New `ConfirmProvider` + `useConfirm()` hook in `@sparx/ui` (driven by Radix AlertDialog under `<ModuleProvider>`), mounted at the dashboard root. Every native `window.confirm()` call swapped for the imperative `await confirm({ title, description, confirmLabel, tone })` API. Tones: `danger` (destructive — Revoke key, Delete asset, Archive variant), `warning` (non-destructive but consequential — Put on hold, Archive pipeline), `module` (live in the current module color — Recompute).
- **Sub-bug found mid-fix (wave 3):** the original 7 swaps had `await confirm()` _inside_ `startTransition(async () => {...})`. React 19 treats setState inside a transition as deprioritised; the `ConfirmProvider`'s setState that mounts the dialog got queued behind the transition and **never painted** — the trigger button just sat disabled until the user gave up. Fixed by ripping `await confirm()` outside `startTransition` in all 6 affected files; the action work stays inside the transition.
- **Verified on prod:** Credit Hold dialog ("Put this account on credit hold?" / Cancel / Put on hold), Recompute dialog ("Recompute segments?" / Cancel / Recompute), Revoke key dialog (`Revoke "Audit verify key"?` / Cancel / Revoke key). All three render the Sparx-styled AlertDialog and fire the action on accept; key revoked end-to-end and badge flipped to Revoked.
- **Screenshot:** `.playwright-mcp/confirm-dialog-credit-hold.png`

### F-13 — Segment detail count stays at 0 after Recompute confirm (new)

- **Severity:** medium (function vs feedback)
- **Route:** `/crm/segments/[id]`
- **Repro:**
  1. `/crm/segments/new` — created `Audit Verify Segment` with rule `customer.totalSpent gte 100`. Preview count returned **1 customer match**.
  2. Click `Create segment` → lands on `/crm/segments/920708c1-fec4-4885-a58f-528e63323387`. Detail page shows `Members 0`.
  3. Click `Recompute` → Sparx AlertDialog appears → click `Recompute`. Wait, refresh. `Members 0` persists.
  4. Detail page renders the read-only Rule card with copy "Built-in segments are read-only; clone to customize. (Rule editor lands in the next dashboard iteration.)" — even though this segment is user-created, not built-in.
  5. Members section renders "Membership is updated as events flow. Place an order or import customers, then check back." — implying the materialised view is event-driven only.
- **Suspected cause:** (a) the detail page's rule renderer treats every segment as built-in (probably defaulting `isBuiltIn` true or missing the read-modify branch); (b) Recompute confirmation invokes `recomputeSegmentsAction` which appears to no-op on a freshly-created segment that has no materialised entries yet, OR runs but doesn't `revalidatePath` the detail route so the member list/count stays stale.
- **Suggested fix:** verify that `recomputeSegmentsAction` actually rebuilds `segment_members` rows for the specific segment (not just registers an event subscription), then `revalidatePath('/crm/segments/[id]', 'layout')`. Separately, drop the "Built-in segments are read-only" copy when the segment's `is_built_in = false` and surface the actual Rule editor instead of the placeholder text.

### F-14 — `sk_live_*` API key surface mismatch (new)

- **Severity:** medium (developer-facing inconsistency)
- **Routes / surfaces:** `https://api.sparx.works/v1/*` (REST), `https://graphql.sparx.works/v1/graphql`, `https://mcp.sparx.works/v1/mcp`, `/settings/ai-integrations` (dashboard issuance)
- **Repro:**
  1. `/settings/ai-integrations` → `Issue key` with label `Audit verify key`. Dashboard returned `sk_live_jswzqnes_r62itf3adzwecjzaywadl5epcxtt3jfy`.
  2. `curl -H "Authorization: Bearer sk_live_..." https://api.sparx.works/v1/crm/customers` → `{ "code": "UNAUTHORIZED", "message": "Invalid or expired token." }`. REST only accepts internal JWTs from the dashboard (`packages/api-core/src/auth.ts:90` calls `request.jwtVerify`, with no `verifyApiKey` path).
  3. `curl ... https://graphql.sparx.works/v1/graphql` → same `"Invalid or expired token."` (same `@sparx/api-core` plugin under the hood).
  4. `curl ... https://mcp.sparx.works/v1/mcp` (POST initialize) → **auth passes** but plan gate kicks in: `{ "code": "RATE_LIMITED", "message": "MCP is not available on the \"starter\" plan." }`. Confirms MCP is the only surface today that accepts `sk_live_*`.
  5. `/v1/openapi.json` exposes a JWT bearer scheme that promises `sparx_live_*` API keys are "Phase 4+". Today's dashboard already issues `sk_live_*` (note prefix mismatch with the spec's `sparx_live_*`) and only MCP honours them.
- **Suspected cause:** auth was wired up per-surface independently. REST + GraphQL took the JWT-only path; MCP took the API-key path; nobody wrote the API-key path into `@sparx/api-core`.
- **Suggested fix:** add `verifyApiKey()` (from `packages/auth/src/api-keys.ts`) as a second branch in `@sparx/api-core`'s preHandler — try JWT first, fall back to API-key verification. Also reconcile the prefix: pick `sk_live_*` _or_ `sparx_live_*` and align (a) the issuer in `packages/auth/src/api-keys.ts` line 18, (b) the OpenAPI security scheme description, and (c) `docs/06-api-specification.md`.

### Carried over from Round 1 (still open, not fixes from this round)

| #    | Status                   | Notes                                                                                                                                                                                                                                 |
| ---- | ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F-06 | open (copy decision)     | OPEN TASKS vs Overdue tasks — pick one wording and align spec + component.                                                                                                                                                            |
| F-07 | open (copy decision)     | Built-in vs System badge — pick one wording.                                                                                                                                                                                          |
| F-08 | architectural note       | No CORS for dashboard origin; no `/api/*` proxy. Document the contract (dashboard uses Server Actions only) or decide otherwise.                                                                                                      |
| F-09 | open (a11y gap)          | Activity rows have no edit affordance and no append-only indicator (CRM activities are intentionally append-only per `feedback_crm_architecture.md` — surface that explicitly so reviewers know edits aren't suppressed by accident). |
| F-10 | open (a11y gap)          | Pipeline kanban: deal reorder via `@dnd-kit` pointer events only. No keyboard / select-menu fallback for moving a deal between stages. Blocks keyboard-only users.                                                                    |
| F-11 | open (form completeness) | `/crm/b2b/new` form is missing the `engineProfiles`, `assignedRep`, `tags`, `creditUsed` fields per the audit prompt §22. Today's form is the minimum-viable subset.                                                                  |

### Mutation walk — what was completed end-to-end on prod

Each row is one successful round-trip against the live tenant `e2e-store`:

| Surface                        | Action                                      | Result                                                                                                                       |
| ------------------------------ | ------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| `/crm/customers/new`           | Created `Audit Verify`                      | `fd4389a3-6c4e-40f9-8fd0-8a7579b08f76` — toast + landed on detail.                                                           |
| `/crm/customers/[id]`          | Added activity note                         | Note rendered in timeline.                                                                                                   |
| `/crm/pipelines/[id]` (kanban) | Created deal `Audit verification deal`      | `f5185af6-fe5c-4ff5-8fae-bbc8e331b524` — landed in Lead column.                                                              |
| `/crm/b2b/new`                 | Created `Audit Fleet Co`                    | `7fc805b4-ab44-4805-96eb-98abbf843ec8` — toast + detail page.                                                                |
| `/crm/b2b/[id]`                | Credit Hold toggle (release → hold → reset) | Sparx AlertDialog renders ("Put this account on credit hold?"), action fires on accept, button label flips. (F-12 verified.) |
| `/crm/segments/new`            | Created `Audit Verify Segment`              | Rule persisted; preview count = 1; detail page rendered. Materialisation count stuck at 0 (F-13).                            |
| `/crm/segments/[id]`           | Recompute confirm                           | AlertDialog rendered + accepted. (F-12 verified; F-13 noted.)                                                                |
| `/settings/ai-integrations`    | Issue key + Revoke key                      | Issued `sk_live_jswzqnes_*`; Revoke AlertDialog rendered + accepted; row flipped to `Revoked` badge. (F-12 + F-14 evidence.) |
| `/settings/modules` → `/crm`   | Deactivate CRM                              | `/crm` flipped to ModuleStub (6 feature cards rendered). Persistence verified across sub-routes.                             |
| `/settings/modules` → `/crm`   | Reactivate CRM                              | All 7 CRM tabs returned.                                                                                                     |

### Non-browser smoke — what landed

| Surface                                                               | Result                                                                                                                |
| --------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| `GET https://api.sparx.works/health`                                  | `200`                                                                                                                 |
| `GET https://api.sparx.works/v1/openapi.json`                         | `200` — valid OpenAPI 3.1 spec exposed. Confirms `sparx_live_*` is documented as Phase 4+ (see F-14 prefix mismatch). |
| `POST https://api.sparx.works/v1/crm/customers` (`Bearer sk_live_*`)  | `401 UNAUTHORIZED` — REST rejects API keys (F-14).                                                                    |
| `POST https://graphql.sparx.works/v1/graphql` (`Bearer sk_live_*`)    | `Invalid or expired token` — GraphQL rejects API keys (F-14).                                                         |
| `GET https://mcp.sparx.works/health`                                  | `200`                                                                                                                 |
| `POST https://mcp.sparx.works/v1/mcp initialize` (`Bearer sk_live_*`) | Auth passes; `RATE_LIMITED — MCP is not available on the "starter" plan`. Confirms key + plan gate.                   |

### Cleanup

- Audit customer `Audit Verify`, deal `Audit verification deal`, B2B account `Audit Fleet Co`, segment `Audit Verify Segment` left in place for the next audit pass — they are useful evergreen seed data on the `e2e-store` tenant.
- `Audit verify key` API key revoked end-of-run.

### What's left

1. **Audit doc round 2 closes the deferred mutation phase from Round 1 (point 2 above).** All formerly deferred mutations exercised; non-browser smoke complete. F-01 confirmed unblocked on prod.
2. **Open work:** F-06 / F-07 (copy decisions), F-08 (architectural decision). All other findings shipped in Round 3.
3. Cron/partition checks and webhook fan-out smoke remain deferred (Cloud SQL touch + read-only cluster scope per CORE rules).

---

## Round 3 — follow-up fixes (2026-05-30)

Continued straight into implementation while the other agents' shell + commerce work runs in parallel. Each fix below is staged locally; push will land once the working-tree typecheck errors from the in-flight shell/commerce work clear.

| #    | Status                  | Fix                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ---- | ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F-14 | **fix shipped**         | `@sparx/api-core`'s `createAuthPlugin` now accepts `Authorization: Bearer sk_live_*` API keys as a parallel branch to JWT verification. Inlined `verifyApiKeyToken` (Prisma + crypto only, no Next/React peerDep pull-in from `@sparx/auth`). Successful API-key auth sets `request.auth = { tenantId, actorId, actorType: 'api', role: 'editor' }`. `@sparx/api-rest` and `@sparx/api-graphql` consume the plugin, so both surfaces now accept the same `sk_live_*` keys the dashboard issues. Schema source `packages/db/prisma/schema/05-api-keys.prisma`; canonical issuer at `packages/auth/src/api-keys.ts` (keep in sync).     |
| F-13 | **partial fix shipped** | Segment detail page no longer renders "Built-in segments are read-only" on user-created segments — text now branches on `segment.isBuiltIn`. Empty-state copy under "No members yet" likewise branches: built-in segments still say "membership updates as events flow"; user-created segments now explicitly say "click Recompute above to evaluate every customer against the rule, then refresh." The deeper materialisation-staleness bug (recompute action runs but the page count + member list stay stale) requires DB-level investigation that the audit prompt blocks (no Cloud SQL touches per CORE). Filed as a follow-up. |
| F-11 | **fix shipped**         | `/crm/b2b/new` form now includes `tags` (comma-separated input, parsed to array) and `engineProfiles` (dynamic add/remove rows with year/make/model/engine-code/count). Submit handler maps the drafts to the wire shape the service expects. `assignedRepId` deferred — surfacing a user-picker needs a new users-list service-layer query, which is a separable change. `creditUsed` is a derived field (managed by orders, not user input) so it's intentionally absent from the form.                                                                                                                                             |
| F-10 | **fix shipped**         | `/crm/deals/[id]` now exposes a keyboard-accessible `<StagePicker>` next to the existing stage badge — native `<select>` of pipeline stages bound to `moveDealStageAction`. Keyboard-only users (and anyone unable to drag) now have a parity path to the kanban's pointer-only DnD. Hidden on closed deals.                                                                                                                                                                                                                                                                                                                          |
| F-09 | **fix shipped**         | `ActivityTimeline` now renders an inline caption — "Activities are append-only — corrections appear as new entries marked Edited." — above the timeline. Makes the locked architectural decision visible to reviewers so the missing inline-edit affordance reads as intentional, not omitted.                                                                                                                                                                                                                                                                                                                                        |
