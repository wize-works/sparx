{
  "name": "@sparx/provider-shippo",
  "version": "0.0.0",
  "private": true,
  "description": "Shippo provider for Sparx Commerce — implements ShippingProvider. Default Sparx Shipping (white-label) and the explicit Shippo install both share this package.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf .turbo dist *.tsbuildinfo"
  },
  "dependencies": {
    "@sparx/commerce-schemas": "workspace:*",
    "@sparx/integration-framework": "workspace:*",
    "shippo": "^2.16.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
