{
  "name": "@sparx/api-client",
  "version": "0.0.0",
  "private": true,
  "description": "TypeScript client for the Sparx public REST API. Thin wrapper around fetch that handles the response envelope, bearer auth, and idempotency-key bookkeeping. Generated types live in `src/openapi.d.ts` (regenerated via `pnpm gen` against the live api-rest /v1/openapi.json).",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./openapi": "./src/openapi.d.ts"
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "gen": "tsx scripts/generate-types.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^25.9.1",
    "openapi-typescript": "^7.4.0",
    "tsx": "^4.19.2",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
