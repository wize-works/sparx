// Placeholder. Run `pnpm --filter @sparx/api-client gen` against a running
// api-rest to populate this file with generated paths/components types.
// Until then, consumers should use the hand-curated types in src/types.ts.

export type openapi_placeholder = never;
