{
  "name": "@sparx/search",
  "version": "0.0.0",
  "private": true,
  "description": "Sparx Search — Typesense client wrapper, collection schemas, indexers for product/variant/order/customer, and the dashboard ⌘K command-palette adapter. Justified deviation from Phase-1 infra to support Gillett Diesel fitment-faceted catalog search from day 1 (docs/22-typesense-search-spec.md).",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./schemas": {
      "types": "./src/schemas/index.ts",
      "default": "./src/schemas/index.ts"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "clean": "rm -rf .turbo dist *.tsbuildinfo"
  },
  "dependencies": {
    "typesense": "^2.1.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
