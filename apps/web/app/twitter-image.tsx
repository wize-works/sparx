// Twitter card uses the same dynamic image as the OG card (summary_large_image).
// Re-export keeps a single source of truth for the artwork.
export { default, runtime, alt, size, contentType } from './opengraph-image';
